\documentclass[a4paper]{article}
\usepackage{tikz}
\begin{document}
Test. This is a relatively long text block that only exists
to show how this paragraph will be layed out with respect to 
the graphic below.
\begin{center}
\input{/tmp/test1.tikz}
\end{center}
Also there should be some text below. This text block
should also be a not too short, so that there will be a line
break within the paragraph.
\begin{center}
\input{/tmp/test2.tikz}
\end{center}
Also there should be some text below. This text block
should also be a not too short, so that there will be a line
break within the paragraph.
\begin{center}
\input{/tmp/test3.tikz}
\end{center}
Also there should be some text below. This text block
should also be a not too short, so that there will be a line
break within the paragraph.
\begin{center}
\input{/tmp/test4.tikz}
\end{center}
Also there should be some text below. This text block
should also be a not too short, so that there will be a line
break within the paragraph.
\begin{center}
\input{/tmp/test5.tikz}
\end{center}
Also there should be some text below. This text block
should also be a not too short, so that there will be a line
break within the paragraph.
\begin{center}
\input{/tmp/test6.tikz}
\end{center}
Also there should be some text below. This text block
should also be a not too short, so that there will be a line
break within the paragraph.
\begin{center}
\input{/tmp/test7.tikz}
\end{center}
Also there should be some text below. This text block
should also be a not too short, so that there will be a line
break within the paragraph.
\begin{center}
\input{/tmp/test8.tikz}
\end{center}
Also there should be some text below. This text block
should also be a not too short, so that there will be a line
break within the paragraph.
\begin{center}
\input{/tmp/test9.tikz}
\end{center}
Also there should be some text below. This text block
should also be a not too short, so that there will be a line
break within the paragraph.
\end{document}
